1. SELECT *
FROM DEPT;

2. SELECT ENAME,JOB,HIREDATE,EMPNO
FROM EMP;

3. SELECT ENAME,JOB,SAL AS 'MONTH_SAL',SAL*12 AS 'ANNUAL_SAL'
FROM EMP;

4. SELECT ENAME + ',' + JOB AS 'EMPLOYEE AND DETAILS'
FROM EMP;

5. SELECT DISTINCT JOB
FROM EMP;

6. SELECT ENAME,SAL
FROM EMP
WHERE SAL >2850;

7. SELECT ENAME,DEPTNO
FROM EMP
WHERE ENAME='SMITH';

8. SELECT ENAME,JOB,HIREDATE
FROM EMP
WHERE HIREDATE BETWEEN '1981-02-20' AND '1981-05-01'
ORDER BY HIREDATE;

9. SELECT ENAME,DEPTNO
FROM EMP
WHERE DEPTNO=10 OR DEPTNO=30
ORDER BY ENAME;

10. SELECT ENAME,JOB,HIREDATE
FROM EMP
WHERE HIREDATE BETWEEN '1982-01-01' AND '1982-12-31';

11. SELECT ENAME,JOB
FROM EMP
WHERE MGR IS NULL;

12. SELECT GETDATE() AS 'DATE';

13. SELECT e.ENAME,e.HIREDATE,m.ENAME,m.HIREDATE
FROM EMP e,EMP m
WHERE e.EMPNO=m.MGR AND e.HIREDATE <m.HIREDATE;

14. SELECT COUNT(JOB) AS 'NO_OF_MANAGER'
FROM EMP
WHERE JOB='MANAGER';

15. SELECT ENAME,DEPTNO
FROM EMP
WHERE DEPTNO IN (20,30,40);

16. SELECT ENAME
FROM EMP
WHERE ENAME LIKE 'T%R';

17. SELECT ENAME,HIREDATE
FROM EMP
WHERE DEPTNO=30;

18. SELECT FORMAT(HIREDATE,'MM-DD-YY') AS 'DATE' 
FROM EMP; 

19. SELECT *
FROM EMP
WHERE JOB='ANALYST' AND HIREDATE='1984-12-03';

20. SELECT ENAME,HIREDATE
FROM EMP
WHERE MONTH(HIREDATE) LIKE '12';

21. SELECT JOB,DEPTNO,AVG(SAL*12) AS 'ANNUAL_SAL'
FROM EMP
GROUP BY JOB,DEPTNO;

22.SELECT COUNT(SAL) AS 'TOTAL_SAL',COUNT(COMM) AS 'TOTAL_COMM'
FROM EMP
WHERE DEPTNO=30;

23. SELECT AVG(SAL),MIN(SAL),MAX(SAL)
FROM EMP
WHERE JOB='CLERK' OR JOB='MANAGER'

24. SELECT DEPTNO
FROM EMP
WHERE EXISTS (SELECT COUNT (JOB) AS CLERK_COUNT
FROM EMP
WHERE JOB='CLERK'
GROUP BY DEPTNO)
GROUP BY DEPTNO;

25. SELECT ENAME,SAL
FROM EMP
WHERE SAL <(SELECT SAL*0.3 AS SAL
           FROM EMP
           WHERE JOB='PRESIDENT');


26. SELECT e.EMPNO,e.ENAME,d.LOC
FROM EMP e,DEPT D
WHERE d.DEPTNO=e.DEPTNO AND d.LOC='CHICAGO';

27. SELECT e.EMPNO,e.ENAME,d.LOC
FROM EMP e,DEPT D
WHERE d.DEPTNO=e.DEPTNO AND d.LOC='CHICAGO';

28. SELECT
E.ENAME,
D.LOC,
D.DEPTNO
FROM EMP E, DEPT D
WHERE E.DEPTNO=D.DEPTNO
ORDER BY LOC;

29. SELECT EMP.ENAME, DEPT.DNAME
FROM EMP, DEPT

30. SELECT AVG(SAL)
FROM EMP;

31. SELECT COUNT(DEPTNO)
FROM EMP;

32. SELECT DISTINCT E.DEPTNO,
DNAME,
LOC,
SUM(COMM) AS TOTALCOMM,
SUM(SAL) AS TOTALOFEACHDEPT
FROM EMP E,DEPT D
GROUP BY E.DEPTNO,DNAME,LOC
ORDER BY DEPTNO;

33. SELECT ENAME,JOB,12*SAL
FROM EMP;

34. SELECT 
ENAME,JOB,COMM
FROM EMP
WHERE COMM IS NULL;

35. SELECT ENAME,JOB,COMM
FROM EMP;

36. SELECT ENAME,JOB,SAL
FROM EMP
WHERE ENAME LIKE '__R%';

37. SELECT ENAME
FROM EMP
WHERE ENAME LIKE '%_____%';

38. SELECT ENAME
FROM EMP
WHERE ENAME NOT LIKE '%_____%';

39. SELECT ENAME
FROM EMP
WHERE ENAME LIKE '%AR';

40. SELECT ENAME, DEPTNO, SAL
FROM EMP
WHERE DEPTNO=30
ORDER BY DEPTNO

41. SELECT ENAME,SAL
FROM EMP
WHERE SAL BETWEEN 1250 AND 1600;

42. SELECT ENAME,SAL
FROM EMP
WHERE SAL < 1250 
OR SAL> 1600;

43. SELECT SUM(SAL)
FROM EMP;

44. SELECT COUNT(ENAME)
FROM EMP
WHERE JOB IS NOT NULL;

45. SELECT MIN(SAL),MAX(SAL)
FROM EMP;

46. SELECT 
ENAME AS EMPLOYEE,
SAL AS SALARY,
JOB AS JOB,
HIREDATE AS 'DATE OF HIRE',
COMM AS COMMISSION,
DEPTNO AS 'DEPARTMENT NO',
MGR AS 'MGR NO'
FROM EMP;

47. SELECT E.ENAME AS EARLIESTDATE,C.ENAME AS LATESTDATE
FROM EMP E,EMP C
WHERE E.EMPNO=C.MGR AND C.HIREDATE <E.HIREDATE;

48. SELECT COUNT(DEPTNO)
FROM EMP
WHERE DEPTNO=10;

49. SELECT ENAME,SAL,COMM
FROM EMP E
JOIN DEPT D
ON LOC='NEW YORK'
WHERE E.COMM >10;

50. a) SELECT DATENAME(WEEKDAY,GETDATE()) 'DAY NAME',FORMAT(GETDATE(),'MM-dd-yy') AS 'DATE'
    b) SELECT DATENAME(WEEKDAY,GETDATE()) 'DAY NAME',FORMAT(GETDATE(),'dd-mm-yy') AS 'DATE'
    c) SELECT DATENAME(WEEKDAY,GETDATE()) 'DAY NAME',FORMAT(GETDATE(),'yy-MM-dd') AS 'DATE'